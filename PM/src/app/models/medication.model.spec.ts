import { MedicationAdministration } from './medication.model';

describe('MedicationAdministration', () => {
  it('should create an instance', () => {
    expect(new MedicationAdministration()).toBeTruthy();
  });
});
